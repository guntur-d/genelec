# This file serves as an example for the required environment variables.
# Copy this file to a new file named .env and fill in your actual values.
# The .env file is ignored by git and should NOT be committed to the repository.

# Your MongoDB connection string (e.g., from MongoDB Atlas)
MONGODB_URI="mongodb+srv://user:password@cluster.mongodb.net/your-db-name"

# A long, random, and secret string used for signing JSON Web Tokens (JWTs)
JWT_SECRET="replace-with-a-long-random-secret-string-for-jwt"

# Your SMTP email credentials for sending password reset emails via Nodemailer
EMAIL_USER="your-email@example.com"
EMAIL_PASS="your-email-or-app-password"

